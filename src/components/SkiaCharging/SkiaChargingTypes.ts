import type { Range } from '../../types';
export interface SkiaChargingProps {
  waveColor?: string;
  adapterColor?: string;
  thunderColor?: string;
  size?: Range<300, 390>;
  backgroundColor?: string;
}
